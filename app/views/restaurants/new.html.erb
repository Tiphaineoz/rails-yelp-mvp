<div class="container col-6">
  <h1>Add a new Restaurant</h1>

  <%= simple_form_for @restaurant do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :category %>

  <%= f.submit "Create restaurant", class: "btn-yellow" %>
  <% end %>
</div>